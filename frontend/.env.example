# Base URL of the FastAPI backend.
# Leave EMPTY for local dev (Vite proxies /api to 127.0.0.1:8000).
# On Render, set this to the API service URL, e.g. https://rag-chat-api.onrender.com
VITE_API_URL=
